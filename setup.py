from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='This is a code and analysis repository for the pap Cell phone tracking data reveals heterogeneity in stay at',
    author='Roman Levin',
    license='MIT',
)
